# Optional: only needed if you enable the LLM phrasing layer.
# The service runs fully without any of these (rule-based by default).

# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional toggle: set to "1" to enable the LLM layer (not implemented by
# default — see the TODO in app/main.py).
# USE_LLM=0
